// The window object that represents the browser window
console.log(window);

// The HTML document loaded in the window
console.log(window.document);

// The <html> element in the document
console.log(document.documentElement);

// The <head> element in the document
console.log(document.head);

